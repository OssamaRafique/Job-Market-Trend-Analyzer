app = "job-market-trend-analyzer-analyzer"
primary_region = "iad"
org = "fantisco"

[build]
  dockerfile = "Dockerfile"

[env]
  HEALTH_PORT = "8080"
  # Rolling window the analyzer aggregates over. 35d = last 5 ISO weeks.
  ANALYZER_WINDOW_DAYS = "35"

[processes]
  app = "python analyzer_consumer.py"

[http_service]
  internal_port = 8080
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
